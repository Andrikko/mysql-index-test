const companies = require('./companies');
const employees = require('./employees');
const events = require('./event');

module.exports = {
    companies: companies,employees: employees, events:events
};